# 2026-09-05 工作日志

## 全站审计与修正（海外大模型定价）+ Gemini 收录口径缩编

### 用户口径（重要，永久生效）
- **Gemini 只保留 3.6 / 3.7 / 3.8 Flash**，其他（3.5 Pro/3.5 Flash/2.5 Pro/2.5 Flash）全站不要了。
- **海外大模型一律没有渠道价**：只有国内大模型才有渠道价。

### 核实结论（官方页 2026-09-05 抓取）
- Gemini 3.8 Flash（09-02 GA）/ 3.6 Flash / 3.7 Flash 同价：入 $0.75 / 出 $3.75 / 缓存读 $0.075 / **缓存存储 $0.5/1M·h**，促销至 2026-12-31，其后翻倍（1.5/7.5/0.15/1.0）。
- Grok 4.6 本地价与官方一致（$2/$6/缓存$0.5；长档 $4/$12/$1），无需改；官方无 cache write。
- DeepSeek 官方只分 CACHE HIT / MISS 两档，无独立「缓存创建」价 → 国内厂商 cache_write 缺失是官方如此，**禁止编造**。
- Google「缓存创建」语义 = cache storage（$/1M tokens/小时），新字段 `cache_storage` 承载。

### 修复落地（commit b713785 + 0e09dd8，已推远程）
1. **抓取器核心 bug**：`scrapers/gemini.py::_effective_price` 无促销时改取**首个（基准档）**价，不再取末位 → 根治「2.5 Pro 档位混搭（2.5/15+缓存0.125）」与「2.5 Flash 音频档 $1.0」类错误。
2. `_is_overseas_model`（GPT/Claude/Gemini/Grok 前缀）：海外模型不产生渠道行、卡片 has_channel_price 恒 False（标「无渠道」）。
3. grok 源 `tier2_skip: true`：价格藏内嵌 JSON（原始值 120000 非换算后 12），audit Tier2 静态核对结构性不适用，消历史 high 误报。
4. `cache_storage` 字段全链路：scraper → store.PRICE_FIELDS → 主流卡片「缓存存储/时」。
5. 9/2 遗留未提交的 notifier `_official_price_alerts` 官方调价专门预警一并补交。
6. Gemini 缩编同步：mainstream_models.yml / openrouter.yml / scrapers 白名单 / site_data 三处列表 / sources.yml note。

### 数据现状
- 抓取：432 记录 / 命中 168 / 审计 **0 high**（此前 1 误报）/ OpenRouter verify ok。
- 海外主流 20：GPT-5.6 Sol/Terra/Luna、GPT-5、GPT-4o、Claude 10 款、Gemini 3.8/3.7/3.6、Grok 4.6。
- 全量测试绿、ruff 干净、站点重建 OK。变更清单报告：`outputs/audit-fix-2026-09-05.html`。

### 坑
- git checkout 还原文件会丢未提交改动（本次 3.8/3.6 新增曾被误还原，需重写 google vendor 段）。
- yaml 结构化删除需保留 vendor 头部；用整段重写（定位 `- id: google` 到 `- id: grok`）最稳。
- Edit 工具对该仓库多文件匹配失败 → 用 python 脚本 `open(p).read()/replace/write` 规避。

## 海外详情面板 + 调价横幅 + 新品检测（commit 待定）

### 用户的 5 个需求
1. 点击海外卡片 → 详情面板应展示对应模型（不再是固定国内列表）✓
2. 海外模型 4 维（输入/输出/缓存创建/缓存读取）+ 多档展示 ✓
3. 官方调价提醒（横幅 + 详情内徽章）✓
4. 新增模型自动检测 + 同厂置顶 + 🆕 角标（CI 自动）✓
5. 不跳 CHANNELS（点击卡片 → 详情面板）✓

### 数据层（core/site_data.py + core/store.py）
- `_normalize_row` 加 `cache_storage` 字段（Gemini）
- `_build_model_details(norm)` 按 canonical 聚合：行内 4 维 + 档位标签（standard / "短文本·≤272K" / "长文本·>272K" / "长上下文·≥200K" 等）+ Anthropic 缓存创建双档派生（5m=cache_write、1h=2×input 官方规则）+ 官方源优先排序
- `_load_official_changes(data_dir)` 读取 `data/official_changes.json`（缺失容错）
- `_build_mainstream_sections` 新增 `new_models` 参数 → 模型 `is_new` 标 + 厂内置顶
- `core/store.py::build_official_changes` CI 抓取时产出 `data/official_changes.json` + `data/known_official.json`
- 关键 bug：`norm` 在 canon 循环里被重新赋值，只保留最后一个 canonical → 改 `all_norm` 累积列表修复
- SOURCE_LABELS 新增 gemini/grok（"Gemini官网"/"Grok官网"），不破坏 openai/anthropic 既有值

### 模板层（core/site_tpl.py + j2 + css）
- `_price_alert_bar(changes, generated_at)` 顶部红色横幅
- `_detail_panel_section()` 默认空态容器
- j2 模板在 metrics 前插 `{{ alert_bar | safe }}`，主流卡片后插 `{{ detail_panel | safe }}`
- style.css：`.price-alert` 横幅 + `.block-detail` 详情面板 + `.detail-table` 等价表 + `.ms-new` 新品角标
- 主流卡片标题行拼 `hot_badge + new_badge + tracking_badge`

### JS（site/assets/app.js）
- `renderModelDetail(canonical)` 从 SITE_DATA.model_details 渲染 4 维分档表 + 缓存创建双档（5m/1h）+ Anthropic/OpenAI/Gemini 各源对照
- `selectOnlyModel` 中跳转改为渲染详情面板（不再跳 CHANNELS）
- `bindPriceAlertClose` 横幅关闭
- 详情面板标题旁若该模型 30 天内有官方调价（mock 内）显示 📈 徽章

### main.py
- archive_snapshot 后调 `store.build_official_changes(DATA_DIR, lookback_days=7, today_iso=today)` → 写 `official_changes.json`
- known_official.json 持久化"首次出现日"，避免首次铺底时把 20 条历史官方源全部误标新品

### 测试
- `tests/test_official_changes.py`（6 例：3 例 build_official_changes + 3 例 _build_model_details）
- 全量 339 例绿、ruff 干净、playwright 端到端验证通过
- 真实抓取：432 记录 / 命中 168 / 0 high / official_changes.json + known_official.json 落地

### 坑
- `norm` 在 `_build_site_data` 的 canon 循环里每次被覆盖 → 加 `all_norm` 累积
- `_OFFICIAL_SINGLE` 的 key 拼接用 `|` 撞上 condition 里的 "空闲时段 | 原厂直供" → 改嵌套 dict `{canonical:{source:{condition:first_seen}}}`
- baseline_path 索引越界 `past[-min-1]` 在 len=1 → 改 `past[max(0, len(lookback))]`
- 首次运行 20 个海外官方源被标新品——已知行为，是"今天我们引入了 20 个官方源"的真实信号；CI 第二天起收敛

## 选区联动（commit 4ff6226，19:05）

- 用户反馈：点海外模型时「国内厂商官方定价」表 + 「国内价格对比」图多余。
- 实现：app.js `setRegionLayout(canonical)`——canonical 前缀 GPT/Claude/Gemini/Grok → 隐藏 `.block-official` + `.chart-card`；点国内模型/未选中恢复。renderModelDetail 开头调用。
- Playwright 验证 4 场景通过（初始显 / 海外隐 / 国内显 / 海外再隐）。已推送远程。

## 海外趋势联动 + 调价事件落盘（commit 8909268，19:15）

- 用户需求：①点卡片（含海外）就能看该模型历史价格趋势 ②海外模型调价都要有落盘记录、与趋势对应。
- 核实：history/*.json 每日快照早已全量含海外模型（23 个×9 天）；缺口在「点卡片不联动趋势」+「调价事件无独立文件」。
- 实现 A（JS 联动）：趋势 IIFE 暴露 `window.__setTrendModel(canon)`；`renderModelDetail` 点卡片时调用，下拉自动切 + 重绘。Playwright 验证 40 模型可选、GPT-5.6 Sol/Gemini 3.7 自动切换 PASS。
- 实现 B（事件落盘）：`store._diff_official_rows(prev, cur)` 官方源 diff（口径同 build_official_changes，含 cache_write/hit/storage，<1% 过滤）；`store.backfill_official_change_log(data_dir)` 遍历 history 相邻快照逐日回填 → `data/price_change_log.json`（append-only，(date,canonical,source,field) 去重幂等，120 天裁剪）。main.py archive_snapshot 后调用。
- 分层语义：history/*.json = 每天价格是多少（趋势源）；price_change_log.json = 哪天谁从 X 调到 Y（事件审计）。互补。
- 测试 +2（回填检出+幂等 / 渠道过滤+无事件），全量 341 绿、ruff 干净。
- 真实数据回填 total=0——历史官方价自 8/22 无变化（准确），模拟场景验证检出 1 条 + 幂等通过。

## 全面复查（commit b2364e8，00:20）

- 逐项对照用户全部需求做系统性复查（数据层+渲染层+JS+端到端），全部 PASS。
- **发现并修复一个真实回归 bug**：趋势图 IIFE 的 `valOf` 引用第一个 IIFE 的局部 `state`
  （USD→CNY 汇率换算），作用域不可见 → drawTrend 抛 ReferenceError；而 renderModelDetail
  开头先调 `window.__setTrendModel` 被异常中断，导致点卡片后详情面板停留在空态。
  - 修复：①第一 IIFE 末尾 `window.__appState = state` ②趋势 IIFE 内 `var state = window.__appState || {rate:7}` ③renderModelDetail 趋势联动包 try-catch。
- **字段完整性核查结论**：GPT-5.5 Pro 官方无缓存价（OpenRouter 权威数据 `input_cache_read`/`cache_write` 均空），GPT-5.5 有缓存读无独立缓存写入（3 列布局）、Grok 无 cache_write——均属"官方规则如此"，非遗漏，禁止编造。
- 12 项综合端到端 PASS、趋势 Chart 实例创建、JS 零报错、319 测试绿、ruff 干净。
