# 2026-07-17

## 自定义模型导入：grok-4.5 (apichatgpt.top)
- 在 `C:\Users\昊哥\.workbuddy\models.json` 新增 entry：`id=grok-4.5`, `vendor=Custom`, url=`https://api.apichatgpt.top/v1/chat/completions`, apiKey 已填入 Bearer token
- 字段：`maxInputTokens=128000`, `maxOutputTokens=8192`, `supportsToolCall=true`, `supportsImages=false`, `supportsReasoning=true`（响应含 `reasoning_content`，确认带推理）
- JSON 校验通过
- 调用测试：HTTP 200，返回标准 chat.completion，content="Hello! How can I assist you today?"
- 注意：首次裸 curl 出现 exit 56 / HTTP 000（本地代理 127.0.0.1:20808 偶发断连），加 `--retry 2` 后稳定成功；控制模型 `gpt-4o-mini` 返回 503（该 provider 不支持），证明端点本身可达
- 待用户重启 WorkBuddy 后，grok-4.5 才会出现在模型选择器中

## grok-4.5 上下文窗口校准
- 原填保守值 128K/8K，已按真实规格校准：maxInputTokens=500000, maxOutputTokens=30000
- 依据：xAI 官方规格（多源一致）Grok 4.5 = 500K 上下文 / 30K 最大输出（对比 Grok 4.3 的 1M/64K 是缩水版）；provider 的 /v1/models 仅确认 grok-4.5 与 grok-4.5-fast 存在，不含窗口信息
- 注意：个别转售商标 16K 输出上限，那是他们自加的更低压，xAI 官方为 30K
- supportsImages 仍保持 false（仅验证过文本通道，vision 透传未实测；Grok 4.5 本身支持多模态，如需可改 true）

## 测试新 key + 模型 bwen3-coder-next —— 失败
- 新 key: sk-FwPOWyj...（与 grok-4.5 的 sk-94EfYa85... 不同）
- 调用 /v1/chat/completions 用 model=bwen3-coder-next → HTTP 503，error code=model_not_found："No available channel for model bwen3-coder-next under group PRO (distributor)"
- 该 key 的 /v1/models 共 16 个模型：gpt-image-1-mini, grok-4.5-fast, codex-auto-review, gpt-image-2, gpt-image-1.5, gpt-5.4-mini, gpt-5.5, gpt-5.6-sol, gpt-5.4, gpt-5.5-openai-compact, gpt-5.4-openai-compact, grok-4.5, gpt-image-1, gpt-5.6-terra, gpt-5.6-luna, gpt-5.2；**无任何 bwen/qwen/coder 模型**
- 结论：key 本身有效（能拉模型列表），但 bwen3-coder-next 不在该 key 的 group 权限内，此 endpoint 不可用该模型。未写入 models.json（避免配置坏项）
- 待用户确认：模型名是否写错 / 是否需要换 key 或换 provider

## 测试 gpt-5.6-sol (新 key) —— 成功，已导入
- 调用 /v1/chat/completions model=gpt-5.6-sol → HTTP 200，content="Hello!"，标准 chat.completion，5.1s 返回
- 已写入 models.json 新增条目：id=gpt-5.6-sol, url 同 endpoint, apiKey=sk-FwPOWyj...(新 key), maxInput=128000/maxOutput=8192（保守默认值，待校准）, supportsToolCall=true, supportsImages=false, supportsReasoning=true
- 该 key 下可用模型清单见上条；gpt-5.6-sol 是其中可用项，已配好

## gpt-5.6-sol token 上限校准 + 完整可用性测试
- 规格来源：OpenAI 官方文档 + dev.to + theplanettools，一致 = **上下文 1,050,000 / 最大输出 128,000**（支持推理 token、图文输入）。注：中文媒体称"150万"不准确，以官方 1.05M 为准
- 已更新 models.json：gpt-5.6-sol maxInput=1050000, maxOutput=128000
- 完整测试：中文真实任务「写带类型注解的快排+复杂度说明+测试代码」→ HTTP 200，13.3s，生成 579 字，返回正确代码，usage 32/626/658，finish_reason=stop ✅ 确认真实可用
- **关键排查经验（可复用）**：用 curl 内联 `-d '{"content":"中文"}'` 在 git bash 下中文会乱码 → 模型收到乱码回"似乎出现乱码"。根因是 shell 传递多字节字符编码损坏，非 endpoint 问题。修复：把请求体写成 UTF-8 文件 + `--data-binary @file` + `Content-Type: application/json; charset=utf-8`，中文即正常。测试非 ASCII prompt 一律走文件法。

## 当前已配置并验证的自定义模型（api.apichatgpt.top）
- grok-4.5（旧 key sk-94EfYa85...）500K/30K ✅ 已测
- gpt-5.6-sol（新 key sk-FwPOWyj...）1.05M/128K ✅ 已测
- bwen3-coder-next（新 key）❌ model_not_found，未配置
