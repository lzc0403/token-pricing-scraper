# 2026-07-17

## 大模型 Token 定价自动抓取项目（software-token-pricing）
- 在 `D:\Projects\WorkBuddy\token 定价` 从零实现：10 源官网抓取 + watchlist 9 目标模型筛选视图 + 周环比 + GitHub Action 每周定时（周日 18:00 UTC）。
- 技术栈：Python + requests + parsel + pyyaml；tencent 文档是 SPA，parser 用 Playwright(`js:true`) 渲染，需 `playwright install chromium`。
- 源清单：aliyun(公开计费文档 fallback)、volcengine、tencent(USD,只取中国大陆/广州区域)、bigmodel、deepseek、minimax、kimi(3个URL)、modelmesh。
- 约束（用户确认）：USD 计价源只取中国大陆区域；阿里云原 console URL 是登录墙已废弃。
- QA 校验发现并修复 3 个 Bug：(A) workflow `git add data/ REPORT.md`→`git add data/`；(B) matcher 删 `or raw_n in alias_n` 分支（消除 GLM-5→GLM-5.1、MiniMax M2→M2.7 误匹配）；(C) workflow issue `--body-file issue_body.md`→`data/issue_body.md`。
- 最终验证：pytest 13 passed；watchlist 精确 9 模型（无 GLM-5/MiniMax M2 残留）；tencent 23 行无新加坡；全量 165 条；货币 CNY+USD，USD×7.2 换算 RMB。
- 维护要点：加网页=改 sources.yml+新 parser；加模型=改 models.yml 别名；阿里云 fallback 地址与 tencent SPA 结构未来可能变，需跟进 parser。
- 未 git commit/push（由用户建空 repo 后 remote add + push）；repo Secrets 可选加 `USD_CNY_RATE`；GitHub 定时任务 60 天无活动会停用。
