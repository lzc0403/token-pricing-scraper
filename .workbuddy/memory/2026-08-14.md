# 2026-08-14

## 全篇遗漏排查与补齐（volcengine_intl + DeepSeek 双币种闭环）

### 发现并修复的遗漏

1. **volcengine_intl 数据未入库**：scraper 修好后只验证了解析，没把真实抓取的 5 行注入 data/，站点里「火山云海外」区块一直为 0。已真实抓取 + 注入 watchlist/prices + 重建站点。
2. **GA 日期后缀未剥离**：BytePlus 页面有 `deepseek-v4-flash-ga-260731`（GA 版本，0731 快照）和 `deepseek-v4-flash-260425`（普通快照）两行，原 `_SUFFIX_RE` 只匹配纯日期后缀，导致 `-ga` 残留、两条 flash 不合并（抓出 6 行）。
   - 修复：拆出 `_GA_SUFFIX_RE`（`-ga-日期`），`_normalize` 循环先剥 GA 再剥普通日期；`_is_base` 同步支持两种后缀。
   - 现 5 行正确：GLM-5.2/GLM-4.7/DeepSeek V4 Pro/Flash/V3.2，flash GA 版归一到 deepseek-v4-flash。
3. **价调整公告行**：页面 row20 是一条「Price adjustment coming soon (Aug 21 2026)」公告，不是价格行，scraper 无价格时已自然跳过（input/output 均 None → continue）。
4. **BytePlus 页面渲染不稳定**：表索引会变（上次 table0、这次 table1），scraper 靠表头特征匹配不依赖索引，正确。
5. **`.prices.prev.json` 残留**：main.py `os.remove` 在沙箱回收站不可用时抛异常中断流程。已删残留 + `.gitignore` 加入口 + main.py try/except 加固（不影响主流程）。
6. **测试补强**：fixture 加 `deepseek-v4-flash-ga-260731` 行 + 新增 `test_volcengine_intl_ga_suffix_dedup`（断言 GA 剥离 + flash 去重为 1 条）。

### 最终状态（220 测试全绿）

- 官方区：DeepSeek V4 Pro/Flash 各 2 行（CNY ¥3/¥6、¥1/¥2 + USD $0.435/$0.87、$0.14/$0.28）
- 渠道区火山云海外（volcengine_intl，USD）：GLM-5.2/GLM-4.7/DS V4 Pro/Flash/V3.2 共 5 行，DeepSeek 标「火山引擎自部署」
- GLM-4.7 渠道行带「最低」标签正常展示

### 遗留

- `aliyun` 源（国内官网）上次 dry-run 0 条，疑似反爬/改版，正式抓取需留意。
- BytePlus 渲染不稳定依赖网络，CI 若不稳定需考虑加重试。