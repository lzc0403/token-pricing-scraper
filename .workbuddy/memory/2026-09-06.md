# 2026-09-06 工作日志

## OpenAI 缩编：下架 GPT-4o + GPT-5 转正（commit 75ac41f）

用户拍板「GPT-4o 不要，只要 GPT-5 以上」。

### 一、下架 GPT-4o（全站 9 处）
- config/openrouter.yml：删 `openai/gpt-4o` 白名单
- config/mainstream_models.yml：删 GPT-4o 目录条目
- config/new_models.yml：删 GPT-4o 监听条目 + checklist 注释改 GPT-5
- core/site_data.py：`MAINSTREAM_SORT_ORDER` 与 `MODEL_ORDER` 删 GPT-4o
- core/site_tpl.py：删 GPT-4o 硬编码 hot 标签（`if r.get("hot") or ... == "GPT-4o"` → `if r.get("hot")`）+ 海外区文案删 GPT-4o
- core/audit.py：`_CRITICAL_MODELS` 删 GPT-4o
- tests/test_mainstream_catalog.py：断言改 `assert "GPT-4o" not in ...`
- **core/site_data.py::_load_history 加 `only_canons` 参数**：历史趋势内嵌数据按当前在册模型过滤，已下架模型（GPT-4o）不再内嵌到 HTML。调用处 `_load_history(data_dir, only_canons=set(canons))`。

### 二、GPT-5 转正（官方价 + 上卡）
关键判断：**OpenAI 官网定价页仍有 gpt-5**（standard 档第 14 行，输入 $1.25 / 缓存 $0.125 / 输出 $10，与 OpenRouter 一致），此前被 openai.py 的 `_MIN_CANON` 主动过滤（只收 5.5+）。
- scrapers/openai.py：`_MIN_CANON` 加 `"gpt-5": "GPT-5"`，`_OR_ID` 加 `"gpt-5": "openai/gpt-5"`，docstring 收录范围改「gpt-5 及以上」
- config/mainstream_models.yml：openai vendor 加 GPT-5 条目（in 1.25 / out 10.0 / 缓存读 0.125，3 列无 cache_write）
- core/site_data.py：`MAINSTREAM_SORT_ORDER` 加 GPT-5

### 验证
- pytest 全绿、ruff 干净、mypy 34 文件 0 错误
- 数据：GPT 系仅存 GPT-5 / 5.5 / 5.5 Pro / 5.6 Sol/Terra/Luna
- GPT-5 官方行 openai in=1.25/out=10.0/ch=0.125 与 openrouter 一致
- 页面 GPT-4o 零残留、GPT-5 卡片已上卡；海外主流 19→20

### 教训
- 下架模型要同时清「历史趋势内嵌」：`_load_history` 读全部 history 快照构建 series，下架模型的旧快照仍会内嵌 HTML。解法是加 `only_canons` 过滤。
- 模型「转正」前要先确认真实官方价是否可抓取（WebFetch 官网定价页），不能拿 openrouter 渠道价硬编码进目录。
