# 2026-09-01

## 每日只读校验（automation-1787338740315，11:22）

- `git pull --ff-only` → Already up to date；`main.py --verify-only` 退出码 0，可疑项 93（high 0 / med 29 / low 64），OpenRouter parsed=38 ok=True suspects=1 high=0。无 Tier1 high，静默成功。

## 收录 GLM-5.3-Flash（13:40）

用户问 GLM-5.3 / GLM-5.3-Flash 是否已加。结论：GLM-5.3 早已收录（8 源），GLM-5.3-Flash 缺失（官网 8/26 快照后上线）。

实测现状（4 源均已上架）：
| 源 | 价格 | 备注 |
|---|---|---|
| zai（智谱海外官方 USD） | $0.075/$0.25，缓存 $0.015 | 限时 5 折至 2026-09-09 |
| bigmodel（智谱国内官方 CNY） | ¥0.4/¥1.4 | 5 折限时两周 |
| openrouter | $0.075/$0.25，缓存 $0.015 | 与官方促销价一致 |
| atlascloud | $0.15/$0.50，缓存 $0.03 | 渠道按官网原价，未跟随促销 |

### 顺带修掉两个解析 bug（促销价没抓对）

1. `scrapers/zai.py`：促销单元格 `<td><del>$0.15</del> $0.075</td>`，`string(.)` 得「$0.15 $0.075」，`clean_price` 取首个数字 → 错抓划线原价，与 OpenRouter 差 100%。加 `_cell_text()` 用 `xpath(".//text()[not(ancestor::del)]")` 排除 `<del>`。
2. `scrapers/bigmodel.py`：①模型名 `name-box` 内首 `<p>` 是纯名、其后 `<div>` 是促销标签「5折限时两周」，粘进名字 → 归一化后 matcher 匹配失败被丢弃；②价格列两个 `.price-line`（折后/划线），`string(.)` 得「1.4元 2.8元」→ 去符号拼成「1.42.8」→ 误读 **1.42**（真实 1.4）。加 `_model_name()` 取首 `<p>`、`_price_text()` 取首 `.price-line`。

两个修复都用「取首个目标节点」策略，促销结束后节点数回落为 1，自动兼容。

### 改动文件

- `config/models.yml`：新增 canonical `GLM-5.3-Flash`（含 `z-ai/`、`zai-org/` 前缀别名）
- `config/mainstream_models.yml`：新增 GLM-5.3-Flash 条目（¥0.4/¥1.4）
- `config/openrouter.yml`：白名单补 `z-ai/glm-5.3`、`z-ai/glm-5.3-flash`
- `config/sources.yml`：更新 zai note（GLM-5.3 已上线，不再是 Coding Plan 专属）
- `scrapers/zai.py`、`scrapers/bigmodel.py`：上述促销价修复
- `tests/test_parsers.py`：补 `recs.append({"model_raw": "GLM-5.3-Flash"})`（该测试断言每个 canonical 都要命中）

全量 pytest 296 passed。端到端探测（4 源 fetch+parse → build_watchlist）确认 4 源全部命中。探测脚本已删，`data/atlascloud_raw.json`/`data/openrouter_raw.json` 被 scraper 内部缓存写入，已 `git checkout -- data/` 还原。

### 顺带修 CI 存量红灯

`core/notifier.py:278` mypy error（`float - float|None`），存量问题非本次引入，但 CI 会跑 `mypy core/ scrapers/ main.py` 并红。修法：把 `official_ps` 的列表推导拆成两步，显式注解 `List[float]`（mypy 无法从生成器推导收窄 None）。

### 门禁

mypy Success（31 文件）/ ruff All checks passed / pytest 296 passed，全绿。
data/ 未被修改（探测脚本写入的 raw 缓存已 `git checkout -- data/` 还原）。

### 已推送

commit `4cb42a9`（18887ee..4cb42a9 → origin/main）。本地与远端原本同点，无需 merge。
CI（北京时间 01:00 每日抓取）下次运行会用新配置，数据进 data/ 后 site 自动生成 GLM-5.3-Flash 行。

想立刻看到：

```bash
gh workflow run scrape.yml
```

## 修复「国内厂商官方定价」错位纳入海外大模型（23:25）

用户截图：OpenAI 的 GPT-5.6-sol 出现在「国内厂商官方定价」分组内。

**根因**：`OFFICIAL_SOURCE` 注册表同时含国内厂商（DeepSeek/GLM/Kimi/MiniMax/Qwen/Doubao）和海外大模型（GPT-5.x），但 `core/site_data.py:_build_site_data()` 第 932-942 行 `official_rows` 填充逻辑只看 `is_official`，没用厂商区域过滤。海外大模型的 USD 官方行因此错位进国内官方表（实测错位 8 行/16 个 noise：GPT-5.6 Sol/Terra/Luna 各 2 条因短/长上下文，GPT-5.5/5.5 Pro 各 1 条）。

**修法**：新增 `_is_domestic_official(canon, r)` helper，在 `_is_official_row` 基础上加一道 `SOURCE_VENDOR[source] ∈ DOMESTIC_VENDOR_ORDER` 过滤。`_build_site_data` 第 933-934 行改用此 helper。海外大模型的 USD 官方行不再进 `official_rows`，由 `_overseas_official_rows()` 单独渲染。

`notifier.is_official_source` 用**前缀匹配**（"GPT"→openai 等），与 `OFFICIAL_SOURCE` 解耦，**不被这次修改影响**——海外大模型的市场行情基准对照照常工作。

**验证**：
- 国内官方表 31 → 12 行，0 海外错位（含 deepseek/bigmodel/kimi/minimax/volcengine）
- 海外官方表 20 行完整（anthropic/google/grok/openai）
- deepseek_us 因 `OFFICIAL_SOURCE` 未映射为官方源是历史现状，与本次修复无关
- notifier 行情基准判定回归 12 例全部正确（GPT/Anthropic/Gemini/Grok/DeepSeek/GLM/Kimi 等）
- pytest 296 passed / mypy Success / ruff passed / HTML 渲染端到端验证通过

**改动**：`core/site_data.py`（+27 -7 行）+ `site/index.html` 自动重生成。

### 用户附带审查发现（未修）

用户说"海外大模型，目前只有官方原厂的报价，没有渠道报价"——**此判断与实际不符**：
- 海外大模型（Claude 20 次、Gemini 6 次、Grok 2 次、GPT-5 系列 10 次）实际**有渠道报价**，显示在「渠道同类报价」section 里，来源主要是 OpenRouter 28 行 + atlascloud 17 行 + aliyun_intl 9 行 + volcengine_intl 5 行。
- 用户可能没看到「渠道同类报价」section，或误把海外大模型官方表（独立分组）当作「国内官方表」看。
- **已修复的 GPT-5.6-sol 错位才是用户截图的根因**，跟"没渠道报价"是两件事。

`notifier.is_official_source` 不识别 `Claude/Gemini/Grok` 为官方源（OFFICIAL_SOURCE 没注册 Anthropic/Google/Grok 的源 id，且 `_OFFICIAL_SINGLE` 只列了 GPT 无 Anthropic/Google/Grok 前缀）。如要启用这三家的市场行情基准对照，需扩 `_OFFICIAL_SINGLE` 加 Anthropic/Google/Grok 前缀 + `OFFICIAL_SOURCE` 补条目——**不在本次任务范围**，待用户拍板。
