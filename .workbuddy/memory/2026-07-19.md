# 2026-07-19 工作日志

## GLM-5.2 定价补全 + 卡片优化 (b8f147e)

### 变更内容
1. **GLM-5.2 定价补全**：从新浪财经核实官方定价（入8/出28/缓存2 元/M tokens），context 从 131K→1M，availability tracking→official
2. **模型排序**：新增 `MAINSTREAM_SORT_ORDER` 常量，按技术先进性降序排列（Kimi K3 → GLM-5.2 → DeepSeek V4 Pro → V4 Flash → V3.2 → ...）
3. **卡片价格紧凑化**：旧的两行「入/出」+ 独立缓存行 → 单行 `入 X · 出 Y` + 同行 `· 缓存 Z`
4. **日期去重**：所有模型 verified_at 相同时，不在每张卡重复显示日期，改为区块顶部统一横幅 `ms-date-banner`
5. **筛选按钮对齐**：`btn-filter-toggle` 从移动端专属（display:none）改为始终可见（inline-flex），与"定价总览"同行

### 技术细节
- `_mainstream_section()` 重写：先 flat 收集全部模型 → 按 sort_order 排序 → 检测日期一致性 → 渲染
- 新 CSS：`.ms-prices-row`（紧凑单行）、`.ms-cache-inline`（同行缓存）、`.ms-date-banner`（顶部日期横幅）
- 废弃旧 CSS：`.ms-prices`、`.ms-price`、`.ms-cache`、`.ms-verified`（卡片内不再使用）
- 204 测试全通过

### 部署
- CloudStudio: https://e1eb515b21984919855c99278bcd51d2.app.codebuddy.work

## 晚间修复 (e29a8b7 → 3064d0b → 3699ddd)

- **隐藏左上角浮动筛选按钮** (e29a8b7)
- **价格展示加单位** (3064d0b)
- **三项综合修复** (3699ddd)：
  1. 对齐：metrics 统计栏从 hero 移入主内容区 sec-metrics，与定价总览同列
  2. 筛选按钮：≡筛选 桌面端切换侧边栏折叠/展开（不再依赖浮动 reopen）
  3. 价格展示：全称（输入/输出/缓存命中）、去¥、单位独立行「元 / 百万 Token」
- 204 测试全通过

## 晚间修复 (0c45f99)

- **筛选按钮恢复**：侧边栏折叠后左上角「筛选」浮动按钮重新显示（`.layout.is-collapsed ~ .sidebar-reopen{display:block}`），水平样式；顶部「≡ 筛选」始终可见，双入口均可重开
- **价格三列同级**：输入/输出/缓存命中 改为 3 列等宽 grid（`.ms-prices/.ms-pcol`），标签在上数值在下，单位「元 / 百万 Token」整块居中
- **上下文去重**：移除卡片右上角「1048K」标签，统一并入 role 行（如「· 1M 上下文」）；新增 `_clean_ctx_label`（1048576→1M, 131072→128K），role 已含上下文则不重复
- 204 测试全通过
