# 2026-08-16 工作日志

## 智谱 Z.ai 海外渠道源完成（Task #120 收尾）

- **修复 `_is_vision` bug**：`scrapers/zai.py` 缺 `import re`（NameError）；正则 `glm-\d+(?:\.\d+)?v` 对 GLM-5V-Turbo/GLM-4.6V-FlashX/GLM-4.5V/GLM-OCR 全部正确过滤（12 case 验证通过）
- **真实抓取验证**：`docs.z.ai/guides/overview/pricing`（requests js:false）成功，12 条 GLM 文本模型（GLM-5.2 $1.4/$4.4/$0.26 ... GLM-4.5-Air $0.2/$1.1/$0.03）
- **fixture + 测试**：保存 `tests/fixtures/zai.html`（279KB），新增 5 个测试（text_models_parsed / vision_models_excluded / currency_usd / glm52_pricing / condition_none）
- **code-review 双轴审核**：
  - Standards：无硬违规；4 个判断式 smell（删除死分支 `"cached input "` / 简化 walrus `re_full` / 抽取表头定位 helper / site.py:668 硬编码 source id）—— 前两个已修，后两个非阻断
  - Spec：发现 Z.ai 被误归 `CHANNEL_SOURCES`，应与 `bigmodel`（智谱国内站）对称归官方源 → **已修正**：zai 从 CHANNEL_SOURCES 移除，`_is_official_any_currency` 扩展支持 GLM + (bigmodel|zai) 双币种官方识别
- **225 测试全绿**，站点重建，commit `f078fee`

## git 仓库损坏修复

- `.git` 对象库物理损坏（`.github/workflows` tree `50750757a2a05cb271f2648d9839d8cd6ed0447e` 不可读 + index cache-tree 损坏 + reflog 损坏）
- 修复方案：`git clone --depth 1 origin /tmp/tps-restore` → `cp -rv .git/objects/* .git/objects/` → `rm .git/index && git read-tree HEAD`
- 后续仍有 `geometric-repack` 警告（更多深层损坏对象），但 commit 成功；建议后续 `git gc --prune=now` 或完整 reclone
