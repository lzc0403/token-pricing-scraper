## 官网基准规则校验 + 真实数据更新（commit 18887ee）

- 校验：全量 300 例测试全绿、ruff 干净；镜像防漂移测试通过。
- 真实运行 main.py：417 记录 / 命中 146 / 变动 8 处 / 审计 0 high / OpenRouter 验证 ok。
- 真实场景验证：MiniMax M3 官网调价（-50%）无渠道跟进 → 触发 🚨 市场行情；DeepSeek V4 Pro 胜算云 +50% 属渠道自调 → 不误报行情。规则符合预期。
- ⚠️ 推送坑：上一轮 `git push --quiet` 实际未成功（远程停在 17b87f5），本次补推 19 个提交到 18887ee。本地 `origin/main` refs 更新有 bug（显示旧值），真实远程状态以 `git ls-remote origin main` 为准。
- 本地无 FEISHU_WEBHOOK_URL，推送跳过（预期行为）；CI 周日 10:00 自动跑时会真实推送。
