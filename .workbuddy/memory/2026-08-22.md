# 2026-08-22 工作日志

## 代码审计修复落地（5 项）+ CI 门禁（全部完成并推送）

按 8/20 审计报告短期行动计划执行，commit `62cbbce` / `bba58c3` / `a09fe56`（已推送 origin/main）：

### 修复内容
1. **atlascloud.py**: `fetched_at` 空字符串 → `datetime.now().isoformat(timespec="seconds")`。
2. **ci.yml + pyproject.toml**（新建）: push 触发 ruff + pytest 门禁。ruff 用 `select=["F","E9","W292"]`，**ignore F401/F841**（存量代码大量 typing.Dict 等风格债 UP006×365，先不启用免得卡死）；行长 E501 也先忽略。
3. **bailian.py**: 缓存命中价 20% 文档明确标注"估算值，非页面明确数据"。
4. **audit.py 新增 2 个检查**:
   - `CACHE_RATIO_ANOMALY`（med）：跨源缓存/输入价比率偏离**中位数基准** >15% 告警。注意：**基准必须用中位数不是均值**——均值会被少数偏离源拉偏导致多数派误报（功能验证抓出的设计缺陷）。
   - `OPENAI_LONG_DEV`（high）/ `OPENAI_LONG_NO_OR`（med）：openai 硬编码长上下文价 vs OpenRouter 同模型交叉校验，偏差 >15% 告警，防硬编码价格过期。
5. **openai.py**: `_LONG_CONTEXT_PRICES` 加 TODO 注释（待动态抓取）。
6. **openrouter.py**: 补 `Tuple` 导入——**ruff F821 抓出的真实未定义名 bug**（第 210 行 `List[Tuple[int,int]]` 之前没 import Tuple，靠 `from __future__ import annotations` 延迟求值才没崩）。
7. **tests/ 新增 3 文件 38 例**: test_currency（13）/ test_openrouter_verify（10）/ test_store（11）——补上之前审计发现的「3 核心模块无测试」缺口。
8. **ruff --fix** 顺带清掉存量 F541（无占位 f-string，site.py 几处）+ W292（文件末尾缺换行，test_audit_gate.py）。

### CI 门禁踩坑（重要教训）
- **第一版 ci.yml 没装 Playwright 浏览器** → pytest 里跑 SPA scraper 测试在 `BrowserType.launch` 挂掉（Executable doesn't exist）。修复：加 `playwright install --with-deps chromium`（与 scrape.yml 一致）。
- **`test_official_rows_vendor_grouped_with_qwen_cache` 依赖真实 data/watchlist.json** → CI 干净环境必挂（aliyun 源线上 0 条导致 Qwen 记录缺失，本地也一样 flaky）。**根治：改用 tmp_path + 合成 watchlist fixture 注入**，不再读真实数据。这个修复让测试从"碰运气"变成"稳定验证分组逻辑"。

### 验证结果
- 本地 pytest: **270 passed, 0 failed**（此前 269+1known-failed → 现在全绿）
- ruff: All checks passed
- **GitHub CI（CI Lint & Test）: ✅ green（49s）**——门禁正式生效，后续 push 自动跑 lint + test

### 中途 .git 再次损坏
stash 触发 refs/heads 丢失（第 4 次了）。按 MEMORY 铁律 reclone + 替换 .git 修复（commit ea882b9 之后），无数据丢失。

## 中期计划 6 项全部落地（commit 5924c66 / fee8568 / b133f3d / 5e7f087，已推送 origin/main）

### #1 site.py CSS/JS 外提（commit 5924c66）
- `_CSS`(382行) → `site/assets/style.css`，`_JS`(716行) → `site/assets/app.js`
- `build_site` 运行时 `_load_asset()` 读取后内联，保持单 HTML 文件部署不变
- JS 占位符 `__SITE_DATA__`/`__PEAK_DATA__` 替换逻辑不动
- **验证核心**：重建 HTML 与重构前字节级一致（仅时间戳差异）
- site.py 2885 → 1779 行

### #3 site.py 按职责拆分（commit b133f3d）
- `core/site_data.py`(988行)：数据组装层（_build_site_data/_overseas_official_rows/_mainstream_sections + 常量）
- `core/site_tpl.py`(782行)：模板渲染层（_stat_card/_table_row/build_site）
- `core/site.py`(51行)：薄入口 re-export，保持 `from core import site` 兼容
- **验证**：重建 HTML 与拆分前字节级一致；270 tests pass
- 历史：2870单体 → 1779(资源外提) → 51/988/782 三模块

### #2 并行抓取 + #4 logging（commit fee8568）
- `run_sources` 拆 fast(js:false, 8源)/slow(js:true, 9源)；快源 `ThreadPoolExecutor(max_workers=6)` 并行（每 scraper 独立 session 线程安全），慢源(Playwright SPA) 顺序执行避免浏览器资源竞争。CI 预计 8min→3-4min
- `main.py` 配 `logging.basicConfig`，`logger.info/warning` 替 print；`base.py run()` try-except 存 `self.error` 不再静默吞异常
- 验证：270 tests pass；`_run_one(deepseek)` 正常返回 3 条

### #6 audit 阈值配置化（commit 5e7f087）
- `config/audit_rules.yml` 新建；`audit.py` 读 yml + 缺省回退默认常量
- `_rule(name, default)` 读取；`_RULES` 模块级缓存
- 阈值：price_max=1000 / rate_tol=0.01 / diverge_ratio=10 / cache_suspect_ratio=0.6 / cache_ratio_dev=0.15 / openai_long_dev=0.15
- **验证**：改 price_max 阈值可即时改变 OUTLIER_PRICE 判定（规则覆盖生效）

### #5 OpenAI 长上下文动态抓取 → 调研结论：暂缓
- WebFetch + 直连页面验证：真实 `TextTokenPricingTables` props 里每行**固定 4 列**（短文本档），长文本档价格不在 rows 数组——通过前端 JS 运行时按模型名查表注入，静态抓取拿不到
- 页面有 4 个 tier 表（standard/batch/flex/fast），均只有短档列
- **决策**：保留 `_LONG_CONTEXT_PRICES` 硬编码（实测与页面真实长档价一致 sol 10/45、terra 4/18、luna 0.4/1.8）+ audit `OPENAI_LONG_DEV` 交叉校验兜底；强行 Playwright 渲染读 DOM 成本高且脆，不划算。记入 #5 任务为「暂缓」

### 最终验证
- 本地 pytest: **270 passed, 0 failed**
- ruff: clean
- **GitHub CI（CI Lint & Test）: ✅ green（48s，run 32515700090）** — 中期全部改动推送后 CI 全绿
- 5 个中期 commit 已推送 `a09fe56..5e7f087`

### 长期计划待做（审计报告第三阶段）
- Jinja2 模板引擎替代手写 HTML 字符串
- 每日抓取 automation 配置（当前仅每周日 GitHub Actions）
- 全量类型注解 + mypy strict CI 门禁
- 历史价格趋势图（Chart.js）
- 价格变动推送通知（feishu/企微 webhook）
## 长期计划收尾 3 项（commit 74ca634，已推送 origin/main）

用户指令"全部做完"——补齐长期计划剩余 3 项（价格推送 / mypy / Jinja2）：

### 价格变动推送（core/notifier.py，新增）
- `build_message` 格式化变动项（canonical/source/字段/old→new+币种）
- `_payload` 区分飞书（msg_type=markdown）与企微（msgtype=markdown）格式
- `notify_price_changes(deltas, date)` 入口：无 webhook env 静默跳过；无 deltas 返回 False
- main.py 在 `report.write_outputs` 之后调用，**独立于审计门禁**（有变动即推送）
- 纯标准库 urllib，无新增重依赖；tests/test_notifier.py 6 例全过

### mypy 类型检查 + CI 门禁
- pyproject 新增 `[tool.mypy]`：warn_unused_ignores / warn_redundant_casts / disallow_untyped_defs=false
- `[[tool.mypy.overrides]]` 对 yaml/parsel/requests/playwright/xlsx 放行缺 stub
- 修复 21 处真实类型隐患（多为 Any|None 参与 min/max/sorted、setdefault 键类型、重复定义 _load_asset、隐式 Optional、yaml 缺 stub 误报 type:ignore）
- scrape.yml 新增 `python -m mypy core main.py` 步骤作为 CI 门禁；.mypy_cache 入 .gitignore
- requirements.txt 增加 jinja2 / mypy

### Jinja2 模板引擎迁移
- 新建 `site/templates/index.html.j2` 承载页面骨架（head/body/sidebar/portal-grid 16 卡片/footer）
- `core/site_tpl.py:build_site` 改用 `Environment(FileSystemLoader, autoescape=select_autoescape(['html','j2']), trim_blocks, lstrip_blocks)` 渲染
- 各区块（metrics/sidebar/official/channel/trend/chart/mainstream）仍由已测 Python 函数生成 HTML，经 `| safe` 注入模板
- 等价性验证：与迁移前基线 `baseline_site.html` 做标签间空白归一化 diff，**仅时间戳不同，零功能回归**
- 顺手删除 site_tpl.py 中重复的 `_load_asset` 定义块（mypy no-redef）

### 最终验证
- ruff: All checks passed
- mypy: Success（13 source files，0 errors）
- pytest: **271 passed**（含 test_notifier 6 例；test_mainstream_ui 浏览器 e2e 由 CI 覆盖）
- HTML 等价 diff 通过
- 推送 `ef136fb..74ca634`

### 长期计划状态
- ✅ 全部 5 项完成（每日抓取 automation / 历史趋势图 / 价格推送 / mypy / Jinja2）
- ⏸ 中期 #5 OpenAI 动态抓取仍暂缓（页面 JS 注入长上下文价，无法静态解析）

## 全项目重审计（glm5.3 从头通读，产出 code-audit-report-v2.html）

以全新视角通读全项目（main.py + core/11 + scrapers/18 + config/6 + tests/9 + CI/2），修正 v1 报告 4 处出入：

1. **「CI 全绿」不完整**：CI 的 mypy 只在 scrape.yml 跑 `python -m mypy core main.py`，**scrapers/ 不在类型检查范围**。实测 `mypy core/ scrapers/ main.py` 爆出 **20 个错误**（aliyun_intl 10 / volcengine_intl 4 / openrouter 5 / tencent 2），全部潜伏在 scrapers/。抽查确认均为**注解债非运行时 bug**（openrouter:217 float vs None、tencent:107/124 索引受行 105 守卫保护），即「潜伏雷」。
2. **OpenAI 动态抓取**：从「暂缓」改为「**永久不可行（by design）**」——页面 Astro 预渲染 props 每行仅 4 列（短文本档），长文本档由前端 JS 运行时注入，静态抓取拿不到；硬编码值（sol 10/45、terra 4/18、luna 0.4/1.8）经核与官网一致，靠 OPENAI_LONG_DEV 交叉校验兜底即可。
3. **硬编码风险盘点**：集中列出 _LONG_CONTEXT_PRICES 与 mainstream_models.yml 目录静态价兜底。
4. **前后差异**：15 项中 14 项已落地，仅 OpenAI 1 项悬置。

### 实测验证
- pytest: **276 passed**（比日志记录的 271 多——含浏览器 e2e 由 CI 覆盖）
- ruff: All checks passed
- mypy（CI 范围 core main.py）: Success 13 files（这就是 CI 绿的原因）
- mypy（全范围 core/ scrapers/ main.py）: **20 errors** ← 盲区实锤
- scrapy 版本陷阱：requirements 未 pin mypy，本地装到 2.3.1 才暴露这些错误，CI 旧版 mypy 未报——版本漂移放大风险

### P0 建议（未落地，仅报告建议）
- 将 mypy 范围扩到 scrapers/ 一次性修 20 个注解债；OpenAI 硬编码文档化为永久不可行。
- 报告：`code-audit-report-v2.html`（保留 v1 原报告，以 diff-style 标注差异）。

## P0-1 落地：扩 mypy 范围 + 修 20 个注解债（已完成并推送 commit `f8cc025`）

按用户指令「按照这个方案进行实施」，将报告第一优先级 P0-1 真正落地：

### 修复内容（全部为注解债，运行时安全，非 bug）
- **aliyun_intl.py (10)**：`out`→`outp`（变量名复用导致类型混淆）；峰谷 `low/high`→`lo_rec/hi_rec`。
- **volcengine_intl.py (5)**：`out`→`outp`。
- **openrouter.py (4)**：`out` 补 Dict 类型注解；去除多余 `# type: ignore`；白名单循环 `m`→`wm`（避免与后续 `_is_free(m)` 冲突，已同步改 `_is_free(wm)`）。
- **tencent.py (2)**：`col["model"]` Optional 索引加 `or 0` 守卫。

### CI 门禁同步
- **ci.yml**：新增 mypy 步骤（push 门禁）。
- **scrape.yml**：mypy 范围从 `core main.py` 扩到 `core/ scrapers/ main.py`（daily 抓取门禁）。

### 验证（全套绿）
- mypy 全范围 `core/ scrapers/ main.py`：**Success: no issues found in 31 source files**（20 错清零）。
- pytest：**276 passed**。
- ruff：**All checks passed**。

### 状态
已推送 origin/main（`56ccdac..f8cc025`）。**git 本地工作区还残留大量脏数据（data/、site/index.html、core/audit.py、memory 等），非本次 commit 内容，未提交——如需清理请单独处理。**

### 脏数据处置（同日补充，已落地）
- **意外发现**：`core/audit.py` 的 OPENAI_LONG_DEV 降级规则（OR 无长档→降 med 不阻断，code OPENAI_LONG_DEV_CH）**此前从未进入任何 commit**，一直潜伏在工作区。已补提交 `04daf39`（真正落地，daily automation 不再误拦 terra/luna）。
- data/ + site/index.html + code-audit-report-v2.html 一并入库，commit `3e0e874`（含 data/history/2026-08-22.json 历史快照归档）。
- 现远程 main = `3e0e874`，工作区仅剩 .gitignore 忽略项（memory/、automations/），干净。
- 我此前把 data/history/ 标成「已忽略」是误判，它实际是项目资产，随 data/ 正常入库。

### OpenAI 硬编码结论（报告 v2 已改）
- 中期 #5 定**永久不可行**（页面长档价由前端 JS 运行注入，静态抓取拿不到），非「暂缓」。硬编码值（sol 10/45、terra 4/18、luna 0.4/1.8）经核与官网一致、未过期，靠 `OPENAI_LONG_DEV` 交叉校验兜底。

## 方案 A 落地：云端唯一数据源（2026-08-22 晚，commit `d94ad7f` + `1a4ec76`）

### 背景
- scrape.yml 08-21 两次 failure（issue #28/#29）：根因 `OUTLIER_PRICE` 误报——GPT-5.5 Pro 的 `output_rmb=1260 > 1000`（RMB 换算放大）被判 high 阻断门禁。HEAD 已含 USD 隔离逻辑（用原始美元价 180 对比），构造函数实测 `gate: pass` 不再误报。
- 用户决策：本机 cron 与 GitHub Action 定位三选一 → **方案 A：云端唯一数据源**。GitHub Action 是唯一权威抓取源；本机 cron 改为纯只读消费。

### 代码改动
- `main.py` 新增 `--verify-only`：不抓取、不写 data/，仅对磁盘现有 data/ 跑 audit 门禁 + OpenRouter 二次验证（供本机 cron 只读消费云端提交）。
- `core/audit.py` run() 增加 `write_audit` 参数（默认 True；verify-only 传 False 不写 audit_report.md/audit.json）。
- `core/openrouter_verify.py` verify() 增加 `write_audit` 参数（同上，纯只读跳过 `_write`）。
- `.gitignore` 忽略 `.workbuddy/automations/`（WorkBuddy 运行时本地产物）。

### 验证（全套绿）
- **纯只读确认**：verify-only 运行前后 data/ 全部文件 mtime 完全一致（零写入）。
- verify-only 结果：可疑项 high 0 / med 29 / low 57，OpenRouter ok=True，退出码 0。
- 全链路 dry-run（17 源）：全量 421 记录，命中 135，high 0 / med 29 / low 57，OpenRouter ok=True → 门禁彻底绿。
- mypy `core/ scrapers/ main.py`：Success, 31 files, 0 issues。pytest：100% 通过。
- 构造函数复现 GPT-5.5 Pro（out_rmb=1260）：tier1_high=0, gate pass, OUTLIER_PRICE=0 → USD 隔离逻辑确认兜住。

### automation 状态
- automation-1787338740315（每日 Token 定价抓取）prompt 改为**只读消费模式**：git pull → verify-only 校验 → 异常上报。约束铁律：绝不运行裸 `python main.py`（会重抓写 data/），绝不 push。push 与权威抓取只属于 GitHub Action。
- 保留每日 10:40 运行（BYHOUR=10;BYMINUTE=40），cwds 不变。

### 状态
已推送 `origin/main = 1a4ec76`（d94ad7f 代码 + 1a4ec76 记忆）。工作树仅剩 data/、site/（dry-run 本机产物，按方案 A 已还原不提交）。
