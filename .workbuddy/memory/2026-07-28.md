# 2026-07-28

## 汇率默认改回 7.0
- `core/currency.py` DEFAULT_RATE 7.2 → 7.0（汇率真相源）；CI 经 `secrets.USD_CNY_RATE` 可覆盖
- `core/site.py` 渲染层默认 7.2 → 7.0；海外行与 `default_rate` 改为跟随 `currency.get_rate()`（单一真相源）
- `data/*` 中 USD 记录的 `input_rmb/output_rmb` 按 7.0 重算（watchlist 18 行、prices 48 行，基于合并后的远程最新周更数据）
- `site/index.html` 重新生成；`tests/test_parsers.py` 默认汇率断言同步 7.2 → 7.0
- 验证：pytest 207 passed；OpenRouter V4 Flash 静态 rmb = 0.63 / 1.26（0.09×7 / 0.18×7）✓

## 阿里云计费口径确认
- 阿里云源为「元/千 Token」，`scrapers/aliyun.py` 已有 `_K_PER_M = 1000.0`，解析时 ×1000 折算为「元/百万 Token」，与全站口径统一。用户问"是否统一折算"，结论：已统一，无需改动。

## Git
- 合并远程周更 `3be176e`（2026-07-20、2026-07-26 两次抓取），冲突的 data/* 与 site/index.html 采用远程最新数据，再用 7.0 重算 rmb 并重新生成站点
- commit `9dc71af`，已 push `origin/main`（3be176e..9dc71af）

## 待办 / 风险
- ⚠️ GitHub Actions 的 `USD_CNY_RATE` secret 若设为 7.2，下次周更会用 7.2 覆盖 data rmb，使线上汇率回退到 7.2。需用户到仓库 Settings → Secrets → Actions 确认该 secret 为空（回退 DEFAULT_RATE=7.0）或显式设为 7.0。
