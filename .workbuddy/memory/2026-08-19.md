# 2026-08-19

## 抓取频率改为每天凌晨1点
- 需求：GitHub Action 抓取从每周一次改为**每天**，时间**凌晨1点（北京时间）**。
- 改动：`.github/workflows/scrape.yml` cron `'0 18 * * 0'`（每周日）→ `'0 17 * * *'`（每天 UTC 17:00 = 北京次日 01:00）。
- commit 信息 weekly → daily。
- 已验证：GitHub 端 workflow state=active，远程文件 cron 确认 `0 17 * * *`。
- 注意：本地领先远程 3 个提交（5e93fbe 后还有遗留改动 core/audit.py、main.py 未提交），本次只提交了 workflow 文件，未触碰其他工作区改动。
- 提交：`6f1e4d6`（1 file changed）。

## 线上价格未刷新排查（14:27-14:42）
- 用户报 https://lzc0403.github.io/token-pricing-scraper/ 价格很多没刷新。
- 根因：**不是抓取问题，是调度周期**。原 cron 是每周日 18:00 UTC 跑一次，最后一次成功 run 是 08-16 18:08 UTC（e2c71d1）→ 线上部署 08-16 数据，页面时间戳 `2026-08-16 18:12:11`。08-17/18 非周日不跑；改成每天 01:00 后第一次触发要到 08-20 01:00 北京。本地 main.py 抓到 397 条新数据但线上靠 CI commit+Pages 部署才刷新。
- 已手动触发 CI run 32223745764 强制刷新线上。
- 发现工作区一批**未提交改动**（audit 门禁功能）：core/audit.py、main.py、tests/test_audit_gate.py、.github/workflows/scrape.yml（门禁告警步骤 + continue-on-error 移除）、data/audit*、data/openrouter_*、site/index.html。等 CI 完成后再推，避免与正在跑的 CI 提交冲突。
