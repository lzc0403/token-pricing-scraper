# 2026-08-11 工作日志

## 完成：SOURCE_LABELS 简化 + 清理冗余 condition 标签

- **SOURCE_LABELS 改名**（`core/site.py` L32-49）：
  - `tencent` → 「腾讯云国际」(原「腾讯云（国际站）」)
  - `tencent_cn` → 「腾讯云CN」(原「腾讯云（国内站）」)
  - `aliyun_intl` → 「阿里云国际」(原「阿里云国际站」)
- **清理三类与 source pill 重复或属内部备注的 condition**：
  1. `scrapers/atlascloud.py`：所有 126 条 condition 由 `AtlasCloud (atlascloud.ai) · USD/1M tokens` → None（pill 已展示 AtlasCloud）
  2. `scrapers/openrouter.py`：23 条 condition 由 `id=xxx | note` 内部备注 → None（id/note 不对外展示；`openrouter_id`/`openrouter_prompt_per_token` 等原始字段保留供 verify）
  3. `scrapers/aliyun_intl.py`：DeepSeek 行保留 `阿里云自部署`；其他模型（GLM/Qwen/Kimi）由 `阿里云国际站 Model Studio (ap-southeast-1)` → None（区域信息已由 SOURCE_LABELS「阿里云国际」表达）
- **保留的 condition**（21 处，均有意义）：原厂直供 / 腾讯云自建 / 阿里云自部署 / 输入长度条件（[0, 256]/[0, 32)/(0, 512k]/≥32k/512k+）/ 0 ≤ 输入长度 ＜ 32k / 1M
- **测试**：213 全过；OpenRouter 二次验证 ok=True；站点重建 346 条/93 命中；残留 tag-cond 仅 21 条有效标签
