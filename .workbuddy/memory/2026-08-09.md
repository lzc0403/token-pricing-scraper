# 工作日志 · 2026-08-09

## 官方定价表黄色背景优化

- 问题：海外/国内厂商官方定价表的 `tr.is-official td` 被设为 `#fff8e7`（amber-soft），导致所有官方行整行泛黄，视觉刺眼、不美观。
- 优化：
  - 移除整行黄色背景，官方行改为白底。
  - 保留左侧 2px 品牌绿标识线（由 amber 改为 `--primary` #2BAE85），与站点主色统一。
  - `.tag-official` 由 amber 黄改为品牌绿软底（`--primary-soft` + `--primary-deep` 文字），与整体 airy-tech 设计语言一致。
  - hover 时官方行背景统一为 `#f8fafb`。
- 验证：Playwright 桌面 1280 截图确认国内/海外官方定价表均变为干净白底；207 tests passed；无横向溢出。
- 部署：commit `b3f2294` + merge rebuild `20829e9` → push → CI run 31267184491 成功部署 Pages。
