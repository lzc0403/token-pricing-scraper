# 2026-08-13

## DeepSeek 官网双站抓取（deepseek + deepseek_us）

- `config/sources.yml` 新增 `deepseek_us` 源：英文站 `api-docs.deepseek.com/quick_start/pricing/`，USD 计费，parser 复用 `deepseek`。
- `scrapers/deepseek.py` 的 `field_of()` 增加英文标签匹配（CACHE HIT/MISS、INPUT/OUTPUT TOKENS），中英两站共用同一解析器；顺序安全：英文分支先判 cache hit/miss，中文分支保持原有逻辑。
- `core/site.py` 新增 `_is_official_any_currency()`：DeepSeek 中文站(deepseek, CNY)与英文站(deepseek_us, USD)均视为官方官网标价；`_normalize_row` 改用它。
- 官方区逻辑改造：CNY 官网行 + USD 官网行都进「国内厂商官方定价」区（deepseek_us 除外，其余厂商 USD 官网行若已有同源 CNY 行则跳过）；图表区只用 official_cny。
- 渲染验证（真实抓取）：官网区 DeepSeek V4 Pro/Flash 各 2 行——CNY ¥3/¥6、¥1/¥2；USD $0.435/$0.87、$0.14/$0.28，USD 行显示原价 + 约¥换算。219 测试全绿。

## 遗留

- BytePlus（volcengine_intl）真实抓取仍 0 条（SPA 表格未渲染，fixture 测试已证明解析逻辑正确）。待解决。
- 本次 dry-run `aliyun` 源 0 条（阿里云官网可能反爬/改版），与代码改动无关；正式抓取时需留意。
- main.py dry-run 收尾 `os.remove(.prices.prev.json)` 在沙箱内被安全 shim 拦截（回收站不可用），非代码 bug，CI 环境无此问题。