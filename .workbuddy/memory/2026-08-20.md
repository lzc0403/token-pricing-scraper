# 2026-08-20 工作日志

## 核查：网页未更新？实际已更新
- 用户反馈「网页没更新」。核查发现：8/19 17:14 UTC（北京时间 8/20 01:14）的定时任务 run `32280494942` **完整成功**（scrape 11m25s + deploy 成功），GitHub Pages 于 8/20 01:26 部署完成。
- 直接 WebFetch 线上页确认数据已是最新：DeepSeek V4 Pro/Flash 峰谷价、GLM-5.3、Gemini 3.7 Flash、Grok 4.6、Opus 5 均在，追踪 30 模型 / 18 数据源。
- 结论：网页数据本身已更新（用户可能看的是缓存或部署前旧版）。真正遗漏的是本地代码修复未推送。

## 未完成任务：本地代码修复被远程挡住
- 上一轮被 429 打断，本地代码改动（workflow timeout 15→30、core/audit.py、scrapers/kimi.py、tests/test_audit_gate.py）只 staged 未 commit 未 push。
- 推送被远程 `4adadb8`（8/19 定时任务成功 commit 的数据）挡住（non-fast-forward）。
- `4adadb8` 仅动 data/ + site/index.html，不碰代码 → 安全 rebase。

## 处理动作
1. 撤销原混合 commit → 去掉 data/site → 只提交代码修复 → `git rebase origin/main` → 推送。
2. 推送 commit `dd0f75b` fix(ci): timeout 30 + audit/kimi 解析修复（在 4adadb8 之上）。
3. `gh workflow run scrape.yml` 触发新运行 `32328690109`（北京时间 11:33），后台监控中（task ojjfD5）。目的：部署改善后代码 + 用修正的 kimi 解析重新生成数据。

## kimi.py 修复要点
- 原 `idx_of("缓存未命中","输入价格")` 会误匹配「输入价格（缓存命中）」列取错价；改为优先精确匹配「缓存未命中」，回退才用「输入价格」。

## 峰谷动态比价完结（commit acb5d08）
- 完成 5 项用户需求：①移除冗余「分组」列 ②溢价基准改官网价 ③峰谷默认闲时优先 ④动态峰谷时钟（按北京时间实时切换）⑤OpenRouter overrides 解析 + 0813 白名单。
- 代码：core/site.py（PARENT_CANON 回退、_source_peak_schedule、PEAK_SCHEDULES、recalc() IIFE）、core/currency.py（enrich 扩展 peak_*_rmb_*）、scrapers/openrouter.py（_parse_overrides UTC HHMM→北京窗口）、config/openrouter.yml（0813 白名单）。
- _build_site_data 兜底调用 currency.enrich：watchlist.json 历史快照缺 peak_*_rmb_*，再跑一次 enrich 补齐，保证下游动态时钟拿到正确值。
- 0813 渠道行验证：input_rmb=4.62(闲)、peak_sched=deepseek_official、official_off_in=4.5（父模型 V4 Pro 回退）。
- 测试 230 全过（aliyun 0 条历史已知失败 deselect）；站点 454KB、107 处 peak-clock/recalc/data-sched 标记。
- git .git/refs/heads 又丢（stash 触发），按 MEMORY 铁律 reclone + 替换 .git 修复。
- 推送 acb5d08 → origin/main，CI run 32346531422 触发。

## 国内主流卡片补齐 GLM-5.3 / Qwen3.8 Max（commit 00245d9）
- 用户指出国内主流卡片区缺 GLM-5.3 和 Qwen3.8 Max。根因：卡片区由 `config/mainstream_models.yml` 独立目录驱动，该文件只有 Qwen3.7 系列和 GLM-5.1/5.2，缺这两个模型（虽然 `config/models.yml` 官方表已有）。
- 修复：mainstream_models.yml 智谱 GLM 组新增 GLM-5.3（8/28 CNY，缓存 2.0，1M 上下文）；通义千问组新增 Qwen3.8 Max（12/36 CNY，缓存 2.5，131K）。
- 验证：卡片区正确渲染（通义千问 3 款、智谱 3 款），价格覆盖正确；231 测试通过；CI run 32355949239 部署成功。

## 待确认
- run 32328690109 是否成功部署（等待后台通知）。

## 用户反馈②：渠道对照表「分组」列与「模型」列重复
- 截图发现「模型」列与「分组」列视觉 100% 重叠（分组渲 canonical，clean_model_name 多源回退到 canonical）。sidebar 已有「模型分类」chips 筛选，该列冗余。
- 删除 `<col w-canon>`、`<th data-key=canon>`、`<td c-canon>`；`w-model` 22%→30%；Excel 导出「模型分组」列保留（导出仍有聚合价值）。
- commit `61d88ed` → 推送 → 触发 run `32328909780`（success，但彼时尚未含后续溢价/闲时修复）。
- 注：run `32328690109` 已先取消（避免旧版部署），改触发 32328909780。

## 用户反馈③：溢价百分比基准 + 峰谷默认闲时优先
- **溢价（+X%）基准**：旧逻辑 `min_in = min(同模型所有行 input_rmb，含渠道)` → 渠道间互相比较。
  改为 `base_in = min(该模型官网行 input_rmb)`（DeepSeek 即 DeepSeek 官网），无官网价则不显示溢价。
  `_normalize_row` 新增 `base_in` 参数；`_build_site_data` 用 `_is_official_any_currency` 筛官网行取基准。
- **峰谷默认闲时优先**：`_merge_peak_rows` 主价字段（input_rmb/output_rmb 等）由取高峰改为取闲时（`mrow = dict(low)`）；
  `_peak_duo` 渲染顺序 闲 X / 高 Y（原 高 X / 闲 Y）；CSS `.px-peak-lo` 改为主色(ink/800)、`.px-peak-hi` 弱化(mute)；`_price_cells` CNY 排序用 `in_low`（闲时）。
- commit `ece27ab`（本地）→ 推送被远程 ea9daee 挡住。

## 重大事故：本地 .git 损坏 + 恢复
- `git pull --rebase` 中途损坏：`.git/refs` 目录与 `.git/logs` 整个丢失，`refs/heads/main` 消失，`objects/ec`（ece27ab 对象）也不在 → `fatal: not a git repository`。
- **地面真相（gh api 查证 origin/main）**：21445cb(daily 2026-08-20) → ea9daee → 61d88ed(删分组列) → dd0f75b(timeout/audit/kimi) 均在 origin。**唯一缺失 = 本地 ece27ab（溢价/闲时修复）**。
- **恢复（遵循项目铁律 reclone + 替换 .git）**：
  1. 备份工作树 `core/site.py`（base_in×6 完好）到 D:/Projects/WorkBuddy/_site_fix_backup.py。
  2. `git clone --depth 5 origin D:/tmp/tps-restore`（中文 temp 路径失败，改 D:/tmp 成功）。
  3. `mv .git .git-corrupted-20260820` + `cp -r D:/tmp/tps-restore/.git .git` 替换。
  4. 仅 `git add core/site.py` 精确提交 `228758e`（1 file, +26/-15），避开 currency.py/AGENTS.md/.workbuddy/memory 等漂移文件。
  5. `git push` → `21445cb..228758e` 成功（fast-forward）。
  6. 取消旧 run 32329736191（已 success，跑在 21445cb 不含修复），触发新 run `32330687024`（跑在 228758e，后台监控 task ZpFBTa）。
- **教训**：中文路径下 git clone/操作易出问题；rebase 冲突/中断会破坏 .git/refs，遇到即 reclone 替换，勿修补。

## 待确认
- run 32330687024 部署结果（预计 ~12 分钟，等待后台通知）。届时线上页：溢价基准=官网、峰谷默认闲时优先。

## 用户反馈④：峰谷比价动态化 + 渠道时区标注（已完成并部署）
- 需求：① 溢价对比随「当前北京时段」动态切换峰谷基准（当前为 DeepSeek 高峰→比高峰价，闲时→比闲时价）；② 渠道（阿里云国际站等）有自己独立峰谷时区，需在渠道页标注，且可能跟国内 DeepSeek 窗口错位。
- **两套峰谷窗口（时区均 UTC+8 但划分相反）**：
  - DeepSeek 官方：高峰 北京 09:00-12:00、14:00-18:00 全价；其余空闲减半。
  - 阿里云国际站：闲时 北京 22:00-08:00；其余忙时。
  - **错峰时段 08-09/12-14/18-22**：官方闲（便宜）vs 阿里云忙（贵）——用户提醒的错位要点。
- **实现（core/site.py，commit `905ac89`）**：
  1. `PEAK_SCHEDULES` 常量 + `channelSched` 映射（渠道→schedule key）。
  2. `_build_site_data` 为每行附加 `official_off/peak_in`、`channel_off/peak_in`、`peak_sched`（官方当前档基准 + 渠道自身档价）。
  3. `_table_row` 渠道行 `<tr>` 注入 `data-sched` / `data-ch-off` / `data-ch-peak` / `data-of-off` / `data-of-peak`，溢价标签加 `js-premium` 类。
  4. 渠道区块顶部加动态 `peak-clock` banner（按北京时间实时判档，每 60s 刷新），peak-note 强化说明两套窗口 + 错峰错位。
  5. `_JS` 注入 `const PEAK = {...}` + `recomputePeak()` IIFE：按当前时段选官方基准档与渠道自身档，重算溢价并着色（高峰红/空闲绿）。
- **验证**：Node 模拟时段（19:30 北京 → 官方空闲/阿里云忙时，溢价 +105.3% 标注「渠道(忙/高) vs 官方(闲/低)」）；Node `--check` 校验 `_JS` 语法；pytest 231 通过 / 1 已知失败（aliyun 线上 0 条，数据依赖）。
- **中途 .git 再次损坏**（stash + pull --rebase 触发）：`refs/heads` 丢失、HEAD object 缺失。已备份 `site.py`（11 处 PEAK 改动完好）→ reclone D:/tmp/tps-restore（HEAD=4f40a74 今日 CI 数据提交，含 228758e）→ 替换 .git → 重新提交推送 `4f40a74..905ac89` 成功。
- **最终部署**：`gh workflow run` → run `32332790002`（北京 12:41 触发，12:45 完成）scrape+deploy 均 success，headSha=`905ac89` 与 origin/main 一致。WebFetch 线上页确认：官方行「闲 0.66/高 1.32 USD」双档、渠道行溢价标签（腾讯云CN V4-Pro +33.3%、胜算云 V4-Flash -66.7%）已按官网基准渲染。
- 清理：`.git-broken-bak`、D:/tmp/tps-restore、D:/tmp/site_peak_fix.py、/tmp/*.js 全部删除。

## 代码审计报告完成
- 完成全量源码审阅：main.py + base.py + site.py(2870行) + audit.py + matcher.py + currency.py + store.py + openrouter_verify.py + mainstream_catalog.py + report.py + 全部 16 个 scrapers + 全部 5 个 test 文件 + 全部 config yml + AGENTS.md。
- 输出 HTML 格式审计报告 `code-audit-report.html`，含 13 项问题分级（0 致命 / 2 高 / 6 中 / 5 低），每项含定位+根因+修复建议；改进规划三阶段（短期止血 / 中期架构解耦 / 长期产品化）。
- 高严重度：①site.py 2870 行巨型单体（CSS/JS/Python 混杂）②openai.py _LONG_CONTEXT_PRICES 硬编码。
- 中严重度：bailian 缓存 0.2 假设 / 17 源顺序执行 / CSS/JS 内嵌字符串 / 无前置输入校验 / 3 核心模块无测试 / atlascloud fetched_at 空。
- 报告为只读审计，未修改任何代码。
