# 2026-08-07 工作日志

## 修复两个抓取 bug（已部署）

### 1. OpenRouter 误用 AtlasCloud 报价
- 根因：之前把用户截图（实为 AtlasCloud 服务商报价 $0.09/$0.18）写进 `config/openrouter.yml` 的 `deepseek/deepseek-v4-flash` 的 `price_override`，覆盖了 OpenRouter API 真实价。
- 修复：删除该 override，V4 Flash 恢复真实 API 价 `0.0882/0.1764`（×1M tokens）。
- 验证：线上 API 返回 prompt=0.0000000882，与解析一致；openrouter_verify 0 suspects。

### 2. 阿里云国际站价格缺失
- 新增 `aliyun_intl` 源（`config/sources.yml`，USD, js:true SPA）+ `scrapers/aliyun_intl.py`。
- 抓取 `modelstudio.console.alibabacloud.com/ap-southeast-1?tab=doc#/doc/?type=model&url=prices`，解析 Qwen/DeepSeek/Kimi/GLM 共 9 模型（去快照/预览后缀，基准档优先）。
- `core/site.py` 接入：SOURCE_LABELS/CHANNEL_SOURCES/SOURCE_VENDOR/OVERSEAS_VENDOR_ORDER，独立「阿里云国际站」海外渠道分组。
- `config/models.yml` 新增 `Qwen3.8 Max` 目标（tests 同步补 synthetic record）。
- 国内阿里云(CNY) 与国际站(USD) 在站点内清晰区分。

### 部署
- commit 58af65a → push → 手动触发 CI (run 31180651989) → scrape ✓ + deploy ✓ → Pages 已更新。
- 测试：207 passed。
- 线上地址：https://lzc0403.github.io/token-pricing-scraper/

## 新增 AtlasCloud 渠道源 + 页面默认 110% 缩放

- AtlasCloud 作为独立渠道源加入来源显示：
  - `config/sources.yml` 新增 `atlascloud` 源，URL `https://console.atlascloud.ai/api/v1/models`（JSON API，USD，无需浏览器渲染）
  - `scrapers/atlascloud.py` 新解析器，取 `type=="Text"` 的 LLM 模型，价格字段 `price.actual.{input_price, output_price, cache_price}`，单位 USD/百万 tokens
  - 写原始缓存 `data/atlascloud_raw.json`，便于二次核对
  - `core/site.py` 接入：`SOURCE_LABELS` / `CHANNEL_SOURCES` / `SOURCE_VENDOR` / `OVERSEAS_VENDOR_ORDER` 添加 `atlascloud`，在海外渠道区以「AtlasCloud」独立分组展示
  - 与现有目标模型对齐后命中 15 条记录（DeepSeek / Kimi / GLM / Qwen / MiniMax / Doubao 等），此前误归到 OpenRouter 的 AtlasCloud 报价现在正确归属显示
- 页面默认缩放 110%：
  - `core/site.py` CSS `html{zoom:1.1}`（保留 `overflow-x:clip`）
  - Playwright 验证 1280/1366/1440 视口均无横向溢出，整体布局无裁切
- 测试：207 passed；commit `62ad83e`；手动触发 CI 部署中（run 31183443528）。
