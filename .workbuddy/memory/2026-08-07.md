# 2026-08-07 工作日志

## 数据更新与站点重新部署

1. 手动触发 GitHub Actions CI（workflow_dispatch）抓取最新数据
2. 第一次 CI（run 31122425443）scrape 成功 8m32s，但 deploy 步骤因 OIDC token 问题失败
3. 拉取远程最新数据（fast-forward 到 8e38509），用本地最新代码（汇率7.0 + portal入口）重新生成 site/index.html
4. 提交（1e539df）并推送到 origin/main 成功
5. 后续连续 5+ 次触发 CI 均因 GitHub Actions runner 分配超时失败（平台问题，非代码问题）
   - 错误信息: "The job was not acquired by Runner of type hosted even after multiple attempts"
   - 每次排队 15 分钟后超时
6. 代码和数据已全部在 origin/main 上，仅 Pages 部署卡在 CI runner 短缺
7. 启动本地预览服务器 http://localhost:8899 作为临时访问方案

## 关键状态
- origin/main HEAD: 1e539df（包含汇率7.0 + portal入口 + 最新CI数据）
- Pages deploy 失败原因: OIDC token（第一次）+ runner 分配超时（后续5+次）
- 线上 Pages 可能仍是旧版本（cae6f09 之前的部署），等 GitHub Actions runner 恢复后自动部署

## ✅ 最终部署成功（2026-08-07 12:10 更新）
- 第 7 次触发 CI（run 31176299376）成功：scrape 3m12s + deploy 20s，全部 ✓
- GitHub Actions runner 恢复后正常分配（之前6次排队超时是平台波动）
- 线上 Pages 已更新到最新：汇率7.0、portal 21入口、V4 Flash 0.63/1.26、阿里云国际站链接均验证通过
- 最新 commit: d694ab9（CI 自动周更 2026-08-07）
- 本地预览服务器（8899）已挂，无需重启——线上已可用
