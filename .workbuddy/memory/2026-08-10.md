# 2026-08-10 工作日志

## 完成：DeepSeek 来源类型 condition 标注 + 渲染层透传

- **问题**：各云厂商 DeepSeek 模型在 watchlist 中 `condition` 字段标注不一致：
  - `tencent_cn` 已正确标「原厂直供」/「腾讯云自建」✓
  - `tencent`（国际站 USD）→ 标 `-` ❌
  - `aliyun_bailian` → `None` ❌
  - `aliyun_intl` → 区域标签「阿里云国际站 Model Studio (ap-southeast-1)」❌
- **修复**（用户要求「未标原厂直供即云厂商自部署」规则）：
  1. `scrapers/tencent.py`：识别 DeepSeek 模型名「原厂直供」后缀；带后缀→`原厂直供`；同名无后缀→`腾讯云自建`；非 DeepSeek 保留 token 长度条件。合并来源类型与 token 长度到 condition（用 ` | ` 连接），按 (model, condition) 去重。
  2. `scrapers/aliyun_intl.py`：DeepSeek（norm 以 `deepseek` 开头）→`阿里云自部署`；其他保留区域标签。
  3. `scrapers/bailian.py`：DeepSeek →`阿里云自部署`；GLM/Kimi/MiniMax → None。
- **渲染层透传**：
  - `core/site.py` `_normalize_row` 加 `condition` 字段。
  - `_table_row` 在「来源」列 `<span class="pill">` 旁加 `<span class="tag tag-cond">` 小标签，不新增表格列。
  - 新增 `.tag-cond` CSS（灰底细字，`margin-left:4px`）。
  - `SOURCE_LABELS`：`tencent`→`腾讯云（国际站）`，`tencent_cn`→`腾讯云（国内站）`，消除重名歧义。
- **验证**：213 测试全过；`data/watchlist.json` DeepSeek 各源 condition 已正确落位；`site/index.html` 渲染出 50 处 `tag-cond`（含「原厂直供」「腾讯云自建」「阿里云自部署」及 token 长度条件）；腾讯云国内/国际站 label 区分可见。

## 完成：补齐国内渠道 — 腾讯云国内站 & 阿里云百炼

- **新增数据源**
  - `config/sources.yml` 增加 `tencent_cn`（腾讯云国内站 TokenHub，CNY，js:true）和 `aliyun_bailian`（阿里云百炼，CNY，js:false JSON 接口）。
  - `core/site.py` 更新 `SOURCE_LABELS` 与 `CHANNEL_SOURCES`，两者归入国内渠道面板。

- **新增解析器**
  - `scrapers/tencent_cn.py`：Playwright 渲染后解析完整语言模型定价表（table #1）。
    - 模型名剥离「正式版/原厂直供/腾讯云自建/HighSpeed/Preview/（…）」注释，保留 `Kimi K3`、`Kimi K2.7 Code` 等多词名。
    - 条件判定：含「原厂直供」→ 原厂直供；同名无后缀行且本表存在原厂直供变体 → 腾讯云自建；否则单档无 condition。
    - 多表 last-wins 去重：完整表（后）覆盖概览表（前），避免 GLM-5.2 被概览价 8/28 覆盖为完整价 10.254/32.2282。
  - `scrapers/bailian.py`：直连 `help.aliyun.com` JSON 接口，解析 274 张定价表。
    - 仅取 deepseek/glm/kimi/minimax 目标家族，跳过「国际/美国/日本」区域行。
    - 缓存命中价按用户给定规则 `input × 20%` 计算。

- **数据与站点**
  - 以远程 `origin/main` 最新 weekly data 为底，精准合并两个新源，生成 `data/prices.json`、`data/watchlist.json` 及 CSV。
  - `site/index.html` 已重建；国内渠道表正确展示腾讯云/阿里云百炼价格，无横向溢出。

- **测试**
  - 新增 `tests/fixtures/tencent_cn.html` 与 `tests/fixtures/aliyun_bailian.json` 离线快照。
  - `tests/test_parsers.py` 增加 6 个测试：腾讯云完整表覆盖、DeepSeek 两档 condition、多词名保留、百炼 20% 缓存、海外区域排除、目标模型命中。
  - 全套件 `pytest -q`：**213 passed**。

- **提交与部署**
  - 提交并合并远程 `origin/main`，推送至 GitHub：`dfeda53..f57cc9f`。
  - 手动触发 Actions：`gh workflow run "Token Pricing Scraper" --ref main`。
    - Run URL: https://github.com/lzc0403/token-pricing-scraper/actions/runs/31385458992
