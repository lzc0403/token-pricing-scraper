# 2026-08-08

- 全面优化「官方定价页」视觉：
  - `core/site.py` 统一设计令牌（spacing/radius/shadow 三级比例），body 背景切为浅灰画布，白卡片浮起。
  - block-card/stat-card/portal-card/hot-card/track-card/model-pick 统一圆角并加轻阴影 + hover 浮起。
  - hero 显示 eyebrow，标题层级更清楚；section/block 间距、字号层级统一。
  - 价格单元格重构（`px-val`/`px-cur`/`js-rmb-hint`），数字/单位/折算三级层次清晰，保留 `js-cny-main` 等 JS 钩子。
  - 补齐此前缺失的 `.market-tabs`/`.market-tab`/`.market-panel`/`.panel-hint` 样式；渠道分页改为胶囊分段控件。
  - 修复 `theme-color` 为绿色 `#2BAE85`（原旧紫色）。
  - 增强 760/480 断点响应式；`prefers-reduced-motion` 禁用 hover transform。
  - 本地 Playwright 验证：桌面 1280 + 移动 390 无横向溢出；207 测试通过；汇率折算/排序/Tab 切换 JS 正常。
  - commit `5ac92ef`，手动触发 CI 部署成功（run 31264182112，deploy 10s）。
